def replace_space(text):
    return text.replace(" ","%20")

text = "Server Side Topics/SQLi/Blind SQL injection with out-of-band interaction/payload.py"
new_text = replace_space(text)
print(new_text)
"gg"


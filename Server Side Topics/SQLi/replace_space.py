def replace_space(text):
    return text.replace(" ","%20")

text = "Server Side Topics/SQLi/SQL injection UNION attack, retrieving data from other tables/payload.py"
new_text = replace_space(text)
print(new_text)
"gg"


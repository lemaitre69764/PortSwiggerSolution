def replace_space(text):
    return text.replace(" ","%20")

text = "Server Side Topics/SQLi/SQL injection UNION attack, retrieving multiple values in a single column/payload.py"
new_text = replace_space(text)
print(new_text)
"gg"

